---
- name: Base Fedora install
  hosts: localhost
  become: true

  tasks:
  - name: Update all packages
    dnf:
      name: "*"
      state: latest

  - name: Install packages
    dnf:
      name:
        - emacs-nox
        - git-core

